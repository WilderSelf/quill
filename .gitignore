# Rust build artifacts
/target
**/*.rs.bk

# Local Claude Code settings (machine-local; project agents/skills/rules ARE tracked)
.claude/settings.local.json

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Note: Cargo.lock IS committed — this workspace produces binaries (app, cli).
