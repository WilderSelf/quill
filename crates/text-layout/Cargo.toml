[package]
name = "quill-text-layout"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

# Real shaping (`rustybuzz`), hyphenation, and bidi arrive in later spec-driven commits.
[dependencies]
